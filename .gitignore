# Compiled engine binaries (wrapper bin/devkit is committed)
bin/devkit-engine
bin/devkit-engine-*
bin/.checksums.*
src/bin/

# Credentials — never commit
*.token
*.secret
*oauth*.json
*credentials*.json
*service-account*.json

# Claude Code local config
.claude/

# Devkit runtime state
.devkit/

# External CLI state (created when contributors use AGY/Antigravity CLI in the repo)
.antigravitycli/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
dist/
*.tsbuildinfo

# Temp
*.log
*.tmp
.worktrees
docs*
codex/docs/
codex/*.md
